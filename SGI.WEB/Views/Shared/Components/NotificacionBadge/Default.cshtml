@model int

<a class="nav-link text-dark" asp-controller="Notificacion" asp-action="MisNotificaciones">
    Notificaciones
    @if (Model > 0)
    {
        <span class="badge bg-danger">@(Model > 9 ? "9+" : Model.ToString())</span>
    }
</a>